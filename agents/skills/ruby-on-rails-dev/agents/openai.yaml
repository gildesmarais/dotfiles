interface:
  display_name: "Ruby on Rails Dev"
  short_description: "Rails overlay for $dev: API, authz, tenancy, testing."
  default_prompt: "Use $dev to implement this Rails change; $ruby-dev and $ruby-on-rails-dev add Ruby and Rails deltas."
