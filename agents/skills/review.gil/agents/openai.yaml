interface:
  display_name: "Review"
  short_description: "Run a focused multi-lens review, optionally publishing a PR COMMENT review."
  default_prompt: "Use $review.gil to review this change: finish baseline plus only the relevant tests, perf, security, and legacy lenses; do not ask me to choose a template."
