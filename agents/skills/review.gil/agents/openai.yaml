interface:
  display_name: "Review"
  short_description: "Run a focused multi-lens review, optionally publishing a PR COMMENT review."
  default_prompt: "Use $review.gil to review this change. For a PR, first ask whether to publish the review. Apply the finish baseline and automatically load only relevant tests, Ruby performance, security, and legacy lenses; always load legacy under quality; do not ask me to choose a template."
