interface:
  display_name: "TypeScript Dev"
  short_description: "TypeScript deltas for $dev"
  default_prompt: "Use $dev to implement and validate this TypeScript change; $typescript-dev adds TypeScript deltas (named types over string/object soup, without over-abstracting)."
