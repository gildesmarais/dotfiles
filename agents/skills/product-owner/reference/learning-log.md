# Learning log

Same-event staging for product-owner harvest candidates. Not progressive-load doctrine — load `SKILL.md` for gate work.

Protocol: [`growth.md`](growth.md) (Growing reference + Ingress). Stage → filter → sparse-promote into `SKILL.md` Doctrine / Doctrine Check / Decision Output / Workflow, or drop. Leave this file empty/thin. No permanent archive of rejects.

## Open candidates

Harvest event: activate `prioritize` branch (SCRUM backlog-ranking under readiness/sequence signals).

1. **[branch:prioritize]** When Priority labels are uniform or untrusted, order by Rank plus readiness axes — do not invent a Priority scale mid-rank.
2. **[branch:prioritize]** Split pullable vs parked/held before ranking; only order within the pullable set.
3. **[branch:prioritize]** Treat refinement readiness as a gate before delivery-capacity ranking — unrefined work is not pullable capacity.
4. **[branch:prioritize]** Prefer documented or evident dependency / phased-sequence order over isolated “value” ranking.
5. **[branch:prioritize]** Park research-blocked clusters until the spike/research outcome unlocks delivery work.
6. **[branch:prioritize]** Work that does not serve a named end-user or operator path → deprioritize or park (cite docs or `unknown`); do not invent golden paths mid-rank — reuse gate doctrine, do not replace it.
7. **[branch:prioritize]** Default output is an ordered list with one-line rationale each; read-only ranking advice unless the user explicitly asks to mutate the board.
