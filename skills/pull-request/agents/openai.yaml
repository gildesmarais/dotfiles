interface:
  display_name: "Pull Request"
  short_description: "Open, slice, retitle, comment, resolve, or reply on GitHub PRs"
  default_prompt: "Use $pull-request to open, slice, or retitle a PR, post an already-verified finding ledger, resolve review feedback, or reply on existing threads. Use $review for read-only or end-to-end review and publishing."
