# refactor-boundaries

**Stub — not authored.**

Do not invent a contract-map playbook, wire/API redesign checklist, or adapter-shell doctrine here.

When only this branch’s signals fire: state the gap in the handoff, keep domain rules out of obvious boundary shells using `deep-modules` / `refactor-types` if those apply, and stop for user direction before inventing a new top-level skill.

Earn a full body only via the branch expansion law in `SKILL.md`.
