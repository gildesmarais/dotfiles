interface:
  display_name: "Open PR"
  short_description: "Commit, push, and open PRs"
  default_prompt: "Use $open-pr to commit my session changes, push the branch, and open a GitHub PR in the browser."
