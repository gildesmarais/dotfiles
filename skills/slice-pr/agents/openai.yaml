interface:
  display_name: "Slice PR"
  short_description: "Rebuild large branches into focused PRs"
  default_prompt: "Use $slice-pr to rebuild this branch into reviewable PRs off the default branch."
